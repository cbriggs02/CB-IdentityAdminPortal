/**
 * @Author : Christian Briglio
 * @Created : 2025
 *
 * AccountStatus - Enum representing the activation state of a user account.
 * Used to distinguish between active and inactive users across the system.
 */
export enum AccountStatus {
  Inactive = 0,
  Active = 1,
}
